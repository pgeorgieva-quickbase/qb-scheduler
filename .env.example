# Solvice API Key — get from platform.solvice.io → Settings → API Keys
# NEVER commit the actual key. Store in .env (gitignored).
SOLVICE_API_KEY=your_solvice_api_key_here

# Mock API port
MOCK_API_PORT=3101
